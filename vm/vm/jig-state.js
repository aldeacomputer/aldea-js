export class JigState {
    constructor (origin, location, stateBuf, moduleId) {
        this.origin = origin
        this.location = location
        this.stateBuf = stateBuf
        this.moduleId = moduleId
    }
}