{
  "name": "aldea-js",
  "packageManager": "yarn@3.5.1",
  "private": true,
  "workspaces": [
    "docs",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -ptv run build",
    "ci": "yarn workspaces foreach -pv run ci"
  }
}
