{
  "name": "aldea-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "mocknet": "mocknet",
    "script": "bash -c 'fn () { TS_NODE_PROJECT=scripts/tsconfig.json TS_NODE_TRANSPILE_ONLY=true node --loader=ts-node/esm --experimental-specifier-resolution=node --no-warnings=ExperimentalWarning scripts/$0; }; fn'"
  },
  "dependencies": {
    "@aldea/cli": "^0.0.1-beta",
    "@aldea/compiler": "^0.0.1-beta",
    "@aldea/sdk": "^0.0.1-beta"
  },
  "devDependencies": {
    "@aldea/mocknet": "^0.0.1-beta",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}